#include "imports.h"
#define CANT_OPS 1

int main(int argc, char *argv[])
{
    sumaMatrices(NULL);

//    Accion *tareas = malloc(sizeof(Accion) * CANT_OPS);
//    char bufferMenu[MAX_LINEA_MENU];
//    FILE *pfConfig = fopen(argv[1], "rt");
//
//    if(!pfConfig)
//        return -2;
//
//    if(!tareas)
//    {
//        fclose(pfConfig);
//        return -1;
//    }
//
//    *tareas = byo;
//
//    menu(pfConfig, bufferMenu, NULL, tareas);
//
//    fclose(pfConfig);
//    free(tareas);

    return 0;
}
